# Leave these blank to run Cohort First in DEMO MODE (in-memory seed data, no login required).
# Fill them in to connect a real Supabase project.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: used only by scripts/admin tasks, never exposed to the browser.
SUPABASE_SERVICE_ROLE_KEY=
